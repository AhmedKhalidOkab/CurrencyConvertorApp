import 'package:flutter/material.dart';

class LisyOfCountry extends StatelessWidget {
  const LisyOfCountry({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 10,
    );
  }
}
